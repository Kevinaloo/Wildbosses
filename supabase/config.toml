# Wildbosses — Supabase project config
project_id = "wildbosses"

[api]
enabled = true
schemas = ["public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[db]
major_version = 15

[auth]
enabled = true
site_url = "https://wildbosses.vercel.app"
additional_redirect_urls = ["https://wildbosses.vercel.app"]
jwt_expiry = 3600
